# Camera display assignments and photographed paper

## Findings on 9 September 2026

The owner reported successful photographs after the D1 shutter and D4 LED rewiring, including battery operation. The hub's inspected diagnostic history contained three completed battery capture events with previousSleepConfirmed=true, matching upload identifiers, HTTP 201 and queue zero. Their preceding sleep durations were 118, 135 and 74 seconds. These are device RTC readings with one-second resolution, not measurements of current or battery life.

The owner also reported a photograph appearing on an unassigned display. The live jobs cache gave both CF7D04 and FB9F18 only picture, sheet and remind. A receipt recorded a photograph displayed on CF7D04. CameraHub.process_one fell back to a picture display when no photo display existed, and the BYOS server also allowed a stored photo layer with the picture job. Both paths now require an explicit photo assignment. This respects the parent's choice and costs the previous automatic display fallback. Existing photo files cannot override that choice on the next display request; the originals remain archived.

On 9 September, camera inventory rows had no job choices. The browser omitted the job group when no choices existed, instead of saying that the camera had no job. A UI test used two camera identities and checked that both remained independently named and listed. Credentials and archive identities remain per camera.

## Paper reading

The owner requested a physical trial using the camera to return a written sheet. Paper collect moments keep the existing scanner source/default, but may also receive a time-bound camera photo. That route loads the exact A4 PNG retained for printing and sends it first, followed by the photograph. A dedicated prompt compares page content, discounts perspective, rotation, lighting and table background, and transcribes visible additions. It reports unreadable portions and requires an explicit uncertainty flag; uncertainty prevents activity advancement. Object/construction collection with source=camera still sends a single image and does not assume a printed reference.

The API preserves the supplied original in photograph mode. Both photographic modes skip the separate page-placement task intended for scans. Tests check exact original pixels at the runner, image order at the model request, both images through the HTTP route, uncertainty and the existing single-image construction path. The physical handwriting trial has not yet occurred. It should use the original already printed for the waiting paper moment, one deliberate photo and inspection of the actual transcription before treating camera acquisition as readable.

Unfinished scan-archive changes remain separate and are not part of this publication.

## Publication and checks

Commit c071f9a passed CI run 34388017303 and was published by API run 34388017243 and panel run 34388017242. Local verification passed 111 focused Python tests, full Ruff, 1,075 Python tests in the worktree with two skips, 143 web tests and the web production build. The worktree count includes the separate unpublished scan-archive test; CI validates the committed tree.

The committed camera_hub, trmnl_byos and run_experience modules were installed on the hub. Both services restarted successfully. Authenticated BMP requests to the running display server verified that CF7D04 and FB9F18, each holding picture/sheet/remind but not photo, no longer receive the stored camera photo layer. The check did not simulate button presses or battery readings. Physical pixels change at each display's next connection. The new paper-reading contract is verified by automated tests; no handwriting legibility trial or new physical capture was performed during this correction.

## Parent-selected returns on 14 September 2026

The owner reported that camera fabrication is complete and requested it as a configurable return channel. The first implementation offered separate primary-return and sheet-reading assignments. The owner rejected that distinction and requested one function, now labelled "mostra a Lanternina" in Italian and "shows Lanternina" in English. The owner chose this wording because "lavoro" did not describe what they wanted the function to convey. The label states the purpose and leaves the material unspecified. Camera and scanner rows offer only `scan`, retaining the existing internal identifier for compatibility. A camera with this assignment receives photographs of both sheets and visible objects. A scanner receives sheets. Legacy `return` assignments are interpreted as the same function, and the panel exposes one removable assignment even when both old values were saved. The `photo` job on displays continues to control where ordinary photographs are shown.

For each return, the hub selects uniformly from all compatible devices assigned to receive work. Several cameras, several scanners, or a mixed selection can receive paper without priority. Objects require a camera. The parent supplies a description that makes each device recognizable, and the instruction uses that description. Unassigned and forgotten devices are excluded. A known inventory is authoritative for input capabilities; missing cached assignments retain the legacy configuration path. One function makes the choice apply equally to all compatible devices; it gives up independently enabling object photographs and sheet photographs on the same camera.

The hub saves the chosen identity, type, name, scanner selector and instruction language with the waiting moment. It keeps them through restarts, help and end-time changes. A new return makes a new choice. A different camera's photo remains in the archive and cannot answer the selected moment. A scanner button press does not scan while that moment waits for a photograph. This buys agreement between the instruction and the receiving device; it costs keeping an unavailable selected device until the current return ends instead of silently switching to another.

The display retains the preceding task instructions and adds the selected return instruction and device name. A fitted renderer keeps both within the 800 by 480 pixel display. The household language reaches the hub with the inventory report. The deviser and continuer describe what should be visible and leave physical device selection to the hub. Existing approved activities retain their old wording and can still mention the scanner; regenerate those activities to obtain device-neutral task text.

The paper reader still receives the exact stored original followed by the photograph. An uncertain reading leaves the activity waiting. Automated tests exercise assignment and removal through the API, one control on each of two camera rows, legacy assignments, selection of each compatible device, parent descriptions, excluded devices, saved identity, instruction language, scanner routing, photographic paper comparison and text bounds. The remaining physical check is a completed sheet returned with the selected camera, followed by inspection of the actual transcription and the display instruction.

### Initial Two-Role Release on 14 September 2026

The owner authorized publication to the existing development environment. The release uses the tested local working tree without a Git commit or push. The full Python suite passed 1083 tests with two skips, all 144 panel tests passed, focused Ruff checks passed, and the production panel build passed with the existing administrator identity variables.

ACR run `dt34` built `lanternina/panel:camera-return-20260914224014`, digest `sha256:b777fd6b2ec92aa696f913655cdc76a6a78e00bebba45cd95ebe593f5845edba`. The API runs this image on ready revision `ca-lanternina-dev-api--0000121`. Its health endpoint returned HTTP 200, and an unauthenticated inventory request returned HTTP 403. The Static Web App serves the new `main-vcBGWxWr.js` and `src-CCloqin_.js` bundles. The latter contains the new return labels and the existing administrator client ID. The deployment explicitly used `web/public/staticwebapp.config.json`; the CLI's first automatic search selected a configuration under `_reference`, so the site was republished with the explicit path and its live content security policy was verified.

The hub received fourteen files, including the existing local photo and scan archive changes required by the tested tree. SHA-256 comparisons passed for every installed file, Python imports succeeded, and `lanternina-camera` and `lanternina-trmnl` restarted successfully. The status service then received the new job choices and Italian language from the live API. The camera remains unassigned and the scanner retains `scan`; publication did not change the parent's choices. Camera firmware was not flashed. No new physical capture was made.

Recovery uses the prior API image `acrlanterninadevssveb.azurecr.io/lanternina/panel:c071f9a`, the previous successful panel source `c071f9ac688292242d6742cfac3759e665ab8c4a` with the existing administrator build variables, and the hub backup `/var/lib/lanternina/backups/camera-return-20260914224014/before.tar`. The backup directory contains lists of prior and newly added paths. Restoring its archive and removing only those newly added paths recovers the prior hub modules; restart the camera and display services afterward. The release source snapshot remains in `tmp/camera-return-20260914224014/source.tar` locally.

Local authentication remains incomplete. The API now permits `http://127.0.0.1:5173` in addition to both existing public origins; preflight requests succeeded for all three and every other API environment variable was preserved. The Entra SPA redirect could not be added: cached administrative authentication returned `AADSTS530035`, and interactive authentication returned `AADSTS500208`. The application registration was not changed. Completing local sign-in requires an administrator of tenant `79825125-8a69-4adc-8eba-831c7decedad` to append that URI to `spa.redirectUris` of application `e80af2eb-6eb5-4524-b8d1-aef90717e10a`, preserving the existing entries. Local CORS permission alone does not resolve the redirect mismatch.

### Single-Function Release on 14 September 2026

The corrected release passed 1089 Python tests with two skips, all 144 frontend tests, focused Ruff checks and the production build. ACR run `dt35` built `lanternina/panel:single-return-20260914232806`, digest `sha256:c36adae79a5f1829c5c4f9ee11f60b3f7969044f96fbdcf3aac1a0f6d3395c36`. The API runs ready revision `ca-lanternina-dev-api--0000122`. A fresh configuration read confirmed all 24 environment values and secret references unchanged; the update response itself omitted values and could not support that comparison. The runtime identity is unchanged.

The live panel serves `main-CMAuBFV0.js` and `src-DZU0v5sJ.js`. The latter contains "riceve i lavori" and omits the old primary-channel wording. The explicit static configuration path was used and the live content security policy remains present. API health returned HTTP 200 and anonymous inventory access returned HTTP 403.

The hub received only `devices/house.py`, `devices/inventory.py` and `shared/capabilities.py`. All three installed hashes and imports passed, both services are active, and the refreshed live cache offers only `scan` on both input devices. At verification the camera was assigned `scan` and the scanner was unassigned. The publication made no assignment writes. No firmware flashing or physical capture occurred.

Recovery can use API image `camera-return-20260914224014`, the saved preceding panel files under `tmp/single-return-20260914232806/panel-before`, and hub backup `/var/lib/lanternina/backups/single-return-20260914232806/before.tar`. The tested source snapshot is retained at `tmp/single-return-20260914232806/source.tar`. Restoring the preceding release also restores its two-role behavior, so it is a technical recovery reference rather than the desired product behavior.