# A four-slide presentation

The presentation lives at https://lanternina.com/it/present/ and https://lanternina.com/en/present/. It shares the public site's deployment, but the site's navigation does not link to it. Both pages declare `noindex, nofollow` and are excluded from the sitemap. This reduces discovery; it does not make either URL private.

The four slides draw on the owner's reference in `site/docs/Lanternina-4-slides-final.pptx`. The owner subsequently moved the system before the activity for this audience: the need and the parent's choice, agents and devices, a paper exchange, and the principles behind the work. The PowerPoint remains the original reference. It was moved from `C:/code/lanternina_ppt` on 15 September 2026, with its SHA-256 checked before and after: `7EE9C53DEC92853C90D3F48B968FE53BC568CE69BAF179FDBBD2D4C29EDA5952`. It is a repository reference, not a file served by the public website. The separate panel and ending slides were removed; their essential points are in the opening and closing speech.

The opening uses the same dog-at-a-desk image as the Mission page, framed around the dog and its drawing. Device photographs appear on the second slide. The third slide pairs the Valle Lunga map and written reply with three steps: prepare, do and return, read and continue. The blue handwriting is identified as simulated. The example sheets remain Italian in both languages.

The reordered speech contains 302 Italian words and 293 English words, counted from the built templates on 15 September 2026. The Italian paragraphs contain 64, 71, 76 and 91 words; the English paragraphs contain 60, 65, 74 and 94. The speaking plan assigns 35 seconds to the opening, 35 to the system, 45 to the example and 55 to the principles, totalling 170 seconds with 10 seconds left for transitions. This is a speaking plan, not a measured delivery. The second paragraph introduces how the activity reaches the home; the third presents Valle Lunga as an example of that exchange. Each slide's notebook button opens its paragraph. Arrow keys, Page Up, Page Down, Home, End, navigation buttons and horizontal touch gestures change slides. A language change preserves the current slide.

The closing slide presents cybersecurity, pedagogy and Responsible AI as design principles. Authenticated access, limited permissions and household separation illustrate cybersecurity. Choice, reasoning and autonomy express the intended educational value, which still requires trials with people. The owner clarified that Responsible AI must address attention as well as content and access: an activity should attract interest and require effort suited to the person, with that effort supporting learning rather than extending use. This is a design requirement, not a measured learning outcome. The existing identity, mission and responsible AI pages supply the fuller account of controls, fairness, inclusion, transparency and human oversight.

The initial sketch described a fixed three-sheet ceiling, vision checks before any human review and a parent-to-child note. The delivered account follows the current public implementation instead: the parent approves the initial activity, automatic checks apply to continuations, and vision reads returned material. The 28-day preference note gives temporary context to the system. The presentation describes sheet limits without conflating simultaneous sheets with the whole game's paper budget. The system overview and implementation status pages supplied these distinctions.

## The panel example

Both public overview pages use `panel-activities-valle-lunga-{it,en}.webp`. These screenshots replace the older six-drawings example. The shortened presentation has no panel screenshot slide. The demonstration asks for a possible railway route from a map, an observation supporting that route and an alternative. It uses the Valle Lunga setting illustrated on the third slide and in the public site. The displayed 45 minutes and two sheets belong to the demonstration fixture; they are not measured completion time or evidence of a household trial.

The screenshots were captured from the current React panel at a fixed 1200 by 900 pixel viewport, using `?preview=showcase#experiences` in development. `SHOWCASE_AFTERNOON` in `web/src/test/fakeApi.ts` supplies invented data; the ordinary test fixture is unchanged. The example uses the scanner return supported by the panel summary. UI language changes between the two captures; activity content remains Italian, as it does for a family's chosen content language. The preview and fixture are excluded from the production build by the existing `import.meta.env.DEV` guard. No family account or live API was used. The new asset filenames avoid the public site's immutable image cache.

## Agents and devices

The second slide shows application agents running in Azure Container Apps and using Microsoft Foundry models. The agents plan activities and continuations, draw pages and interpret photographed replies. The hub follows the plan, controls timing and coordinates local devices. Following the PowerPoint reference, the diagram uses distinct routes: a rose arrow delivers the plan to the hub and branches to printer and display; a blue arrow returns photographs from the camera to the hub, and another continues from the hub to the agents. The hub is the intermediary in both directions. Labels and an accessible diagram description distinguish the routes as well as colour. On mobile, the same routes run vertically between the agents, hub and device row. The hub initiates cloud connections; the diagram does not imply inbound Azure access to the home or deployment on Foundry Agent Service. The public execution and data flows page supplies these distinctions.

The owner provided assets in `C:/code/lanternina_ppt`. `stampante.jpeg` is an illustration, not a photograph of the installed printer. `display con istruzioni.jpeg` shows the display with Valle Lunga instructions. Both were resized without cropping, with EXIF orientation applied, and encoded as WebP at quality 84. The exported printer is 960 by 715 pixels and 48,388 bytes; the display is 960 by 749 pixels and 44,962 bytes, measured from the exported files. `AI Foundry - 18px.svg` was copied unchanged as the cloud's product icon. The existing public hub and camera photographs complete the view. The display's content remains Italian in both languages.

The three-stage layout gives agents more space than the hub and includes all three requested devices. It costs some image size compared with a dedicated hardware slide. The four-slide version was checked at 1280 by 720 and 1440 by 900 pixels without scrolling. Mobile layouts at 390 by 844 and 320 by 640 pixels stack the stages and scroll vertically.

## Annotations

The owner supplied the portable reference now stored in `site/docs/WHITEBOARD.md`. Its original content is preserved. `site/src/scripts/whiteboard.ts` adapts it for the presentation only. Each slide has an SVG overlay inside its scrolling area. Pen proximity arms the overlay and disables pen panning; mouse and touch restore ordinary navigation unless the mouse-drawing checkbox is enabled. The eraser removes whole nearby strokes. Width uses the pressure at the start of a stroke, matching the reference rather than claiming continuously variable brush pressure.

Colours, width, undo and clear belong to a collapsible toolbar. Clear affects only the current slide and asks for confirmation. Undo also restores erased or cleared strokes, with a maximum of 50 action snapshots per slide in memory. Restored strokes can be undone after reloading.

Ink is saved under `lanternina-presentation-ink-v2:it` or `lanternina-presentation-ink-v2:en` in the browser's local storage. The new version separates this four-slide sequence from the six-slide version: previous `v1` entries remain untouched, rather than placing their ink on different content after renumbering. Ink is never uploaded. Storage failure leaves drawing available in memory, so persistence depends on browser permissions and available space. Each record contains only recognised colours, a bounded width and finite normalised coordinates. The languages have separate records because translated layouts differ. Coordinates scale with the slide's content area; annotations are not attached to individual words, so a responsive reflow can change their alignment. Use the intended presentation viewport when annotating.

The reordered system and paper slides declare `data-ink-page` with their original `v2` keys. Loading and saving use those keys while navigation uses the current slide numbers. Existing drawings therefore follow their content after the swap; the first and fourth slides retain their original keys.

## Verification

Run `npm --prefix site run build` and `python site/check-build.py`. Restart Astro preview after rebuilding: the preview process used here retained an earlier build until restarted. Browser checks cover image loading, slide navigation, language continuity, notes, pen and eraser events, pressure-derived width, optional mouse drawing, touch pass-through, local persistence, slide isolation, undo and clear. Check 1440 by 900 and 1280 by 720 pixels for projection, and 390 by 844 and 320 by 640 pixels for mobile layout.

Synthetic pointer events verify the browser engine; they do not establish Wacom driver behaviour or physical pen performance. A physical front-tip/back-tip trial remains for the owner. This document records implementation choices, not evidence of educational benefit.