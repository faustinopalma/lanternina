# Material correspondence

On 15 September 2026, the owner requested games with several exchanges before closure. Lanternina sends printed pages and display messages. The adolescent replies by writing or drawing on the sheets and photographing the work. Existing input channels, output channels, JSON contracts and execution actions remain unchanged.

The idea editor, deviser, continuer and runtime agent now ask for this structure. A received contribution changes the next response. Pages arrive across exchanges instead of presenting the whole game at once. Each game retains an explicit ending, and an early stop remains possible. The prompts account for image processing, printing and display refresh waits without adding artificial delays or promising immediate replies. This makes the response depend on material the adolescent produced, at the cost of waiting between contributions.

The default sheet preference changes from 2 to 5, with choices from 1 through 5. Existing saved choices remain unchanged. The deviser states the requested whole-game ceiling in the existing script field, so continuations and runtime decisions can read it without a new contract. Returned sheets still count, and any printed closing response uses the same allowance. The continuer and runtime agent use five as a fallback for older scripts that do not state a ceiling.

The whole-game ceiling is a prompt instruction, not a new enforced print quota. The existing structural check still bounds simultaneous sheets; it does not count actual prints across continuations. Changing that enforcement would require a separate execution change. An old script without a recorded ceiling cannot recover the household's former choice through these unchanged inputs. The default does not rewrite activities already approved or change saved household settings.

The parent panel warns when at least one display is listed and every listed display has a saved connection interval greater than 5 minutes. Missing per-device intervals retain the existing 10-minute fallback. Cameras, printers and removed devices do not satisfy or trigger the condition. An interval of exactly 5 minutes suppresses the warning. The warning asks for an activity display at 5 minutes or less; it does not block saving or alter assignments. Shorter intervals reduce refresh waits and consume more battery wakeups, without bounding model or print latency.

Verification uses the existing prompt-rendering snapshots, preference and agent tests, and panel interaction tests covering 5 and 6 minutes, missing intervals, no displays, removed devices, saved edits and failed saves. Static builds and browser checks cover the panel and both public language trees. These checks verify instructions and software behavior; a generated multi-exchange game and a physical afternoon still need to be tried.