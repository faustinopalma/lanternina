diff --git a/platformio.ini b/platformio.ini
index 56de1a5..92f2eb2 100644
--- a/platformio.ini
+++ b/platformio.ini
@@ -628,7 +628,6 @@ build_flags =
 	${env:esp32_base.build_flags}
 	-D BOARD_XIAO_EPAPER_DISPLAY
 	-D PNG_MAX_BUFFERED_PIXELS=6432
-	-D USE_FAKE_BATTERY_VOLTAGE
 	-D MINI_EPD
 
 [env:seeed_sticky]
diff --git a/src/DEV_Config.h b/src/DEV_Config.h
index 497cb4f..6acacbb 100644
--- a/src/DEV_Config.h
+++ b/src/DEV_Config.h
@@ -138,8 +138,11 @@
 #define EPD_RST_PIN  38
 #define EPD_DC_PIN   10
 #define EPD_BUSY_PIN 4
-   // DEBUG - remove the fake battery line after testing
-#define FAKE_BATTERY_VOLTAGE
+   // Lanternina: upstream left FAKE_BATTERY_VOLTAGE defined here, with its own note to
+   // take it out after testing. It never came out, so this board reported exactly 4.2 V
+   // for ever and every thing downstream that reads a battery was dead. The real read is
+   // in readBatteryVoltage() just past the fake one: PIN_VBAT_SWITCH 6 on, eight samples
+   // of PIN_BATTERY 3, averaged and doubled for the divider.
 #elif defined(BOARD_TRMNL_X)
 //   #define FAKE_BATTERY_VOLTAGE
 
