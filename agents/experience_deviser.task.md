<!--
The first line of the whole prompt, and the last block of it: what the household is, quoted as material. The placeholders are filled in agents/experience_deviser.py::the_prompt, which is the one place that decides what of a household reaches a model.

Everything a parent typed arrives as JSON. That is not decoration: a quoted list cannot be mistaken for a continuation of the instruction above it.
-->
Devise one afternoon for one household, from nothing.
