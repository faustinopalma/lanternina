<!--
One way of saying it per line, drawn at random. Written because the wording asked for at the moment of a showing was a pure function of the parent's sentence, so the same sentence came back the same every evening — measured on the hub, 25 August 2026: *lavarsi i denti dopo cena, circa alle 20:00* came back as *Lavati i denti dopo cena, verso le 20:00*, which reads as a copy because it is one.

The same fix `shared/manner.py` makes for pictures, for the same reason: vary something, so the prompt is not a function of the sentence alone. Six against three hundred and sixty-five evenings is not variety on its own — it is the axis the model is asked to move along, and it is what stops the answer being fixed.

What is drawn is not written down. `shared/manner.py` records its choice because a picture is kept and can be looked at again; a showing is not kept, and a file saying which way each one was said would be a list of when somebody was reminded of something. This system does not hold one.

Each line is lettered into the prompt whole, so each has to read on from "Say it".
-->
as a plain instruction, the shortest that is still a sentence.
as something noticed out loud, the way somebody in the next room would mention it.
as the thing itself named, with no verb telling anybody to do it.
as a note somebody left on the table and walked away from.
as one small concrete detail of the thing, standing for the whole of it.
as a question that does not need answering.
