<!--
Kept as its own file because it is the part that was measured. Without it the four came back as one sentence with the verb swapped — "Metti / Sistema / Riponi / Inserisci il libro di storia nella cartella" — which is not what four are for. `tools/probe_wording_variety.py` compares the instruction with and against without it.
-->
The same reminder is shown day after day, so the $how_many must not be one sentence with a word swapped. Two that differ only by a synonym count as one.
